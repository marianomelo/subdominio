---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// Esta página manejará las URLs dinámicas como /blog/optimizacion-performance-wordpress
export async function getStaticPaths() {
  // Obtener posts desde las colecciones de contenido
  const allBlogPosts = await getCollection('blog');
  
  return allBlogPosts.map((post) => ({
    params: { slug: post.data.slug || post.slug },
    props: { post }
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();

const formatDate = (dateString) => {
  const date = new Date(dateString);
  return date.toLocaleDateString('es-ES', { 
    day: 'numeric', 
    month: 'long', 
    year: 'numeric' 
  });
};

const getCategoryColor = (color) => {
  const colors = {
    blue: 'bg-blue-100 text-blue-700',
    orange: 'bg-orange-100 text-orange-700',
    green: 'bg-green-100 text-green-700',
    purple: 'bg-purple-100 text-purple-700',
    red: 'bg-red-100 text-red-700'
  };
  return colors[color] || 'bg-gray-100 text-gray-700';
};
---

<Layout title={post.data.title} description={post.data.excerpt}>
  <!-- Hero del artículo -->
  <section class="pt-32 pb-16 bg-white">
    <div class="container mx-auto px-6">
      <div class="max-w-4xl mx-auto">
        <div class="mb-8">
          <a href="/blog" class="inline-flex items-center text-gray-600 hover:text-black transition-colors duration-200 mb-6">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
            </svg>
            Volver al blog
          </a>
        </div>
        
        <div class="flex items-center mb-6">
          <span class={`px-3 py-1 text-sm font-medium rounded ${getCategoryColor(post.data.categoryColor)}`}>
            {post.data.category}
          </span>
          <span class="text-gray-500 mx-4">•</span>
          <span class="text-gray-600">{formatDate(post.data.publishedDate)}</span>
          <span class="text-gray-500 mx-4">•</span>
          <span class="text-gray-600">{post.data.readingTime} min de lectura</span>
        </div>
        
        <h1 class="text-4xl md:text-5xl font-medium mb-6 text-black leading-tight">
          {post.data.title}
        </h1>
        
        <p class="text-xl text-gray-600 leading-relaxed mb-12">
          {post.data.excerpt}
        </p>
        
        <div class="flex items-center mb-12">
          <div class="w-12 h-12 bg-gray-200 rounded-full flex items-center justify-center mr-4">
            <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
          </div>
          <div>
            <p class="font-medium text-black">{post.data.author}</p>
            <p class="text-sm text-gray-600">Equipo de desarrollo de Subdominio</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contenido del artículo -->
  <article class="pb-16">
    <div class="container mx-auto px-6">
      <div class="max-w-3xl mx-auto">
        <div class="prose prose-lg max-w-none">
          <Content />
        </div>
      </div>
    </div>
  </article>

  <!-- Compartir y navegación -->
  <section class="py-12 bg-gray-50 border-t border-gray-200">
    <div class="container mx-auto px-6">
      <div class="max-w-3xl mx-auto">
        <div class="flex flex-col md:flex-row justify-between items-center">
          <div class="mb-6 md:mb-0">
            <h3 class="font-medium text-black mb-3">Comparte este artículo</h3>
            <div class="flex space-x-4">
              <a href="#" class="text-gray-600 hover:text-black transition-colors duration-200">
                Twitter
              </a>
              <a href="#" class="text-gray-600 hover:text-black transition-colors duration-200">
                LinkedIn
              </a>
              <a href="#" class="text-gray-600 hover:text-black transition-colors duration-200">
                Facebook
              </a>
              <a href="#" class="text-gray-600 hover:text-black transition-colors duration-200">
                Copiar enlace
              </a>
            </div>
          </div>
          
          <div class="text-center md:text-right">
            <p class="text-sm text-gray-600 mb-2">¿Te gustó este contenido?</p>
            <a 
              href="/blog" 
              class="inline-flex items-center px-4 py-2 bg-black text-white font-medium hover:bg-gray-800 transition-colors duration-200"
            >
              Ver más artículos
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA final -->
  <section class="py-16 bg-black text-white">
    <div class="container mx-auto px-6 text-center">
      <div class="max-w-2xl mx-auto">
        <h2 class="text-3xl font-medium mb-4">
          ¿Necesitas ayuda con tu proyecto?
        </h2>
        <p class="text-gray-300 mb-8">
          En Subdominio tenemos la experiencia y conocimiento para llevar tu negocio al siguiente nivel. 
          Hablemos sobre cómo podemos ayudarte.
        </p>
        
        <a 
          href="/contacto"
          class="inline-flex items-center px-8 py-3 bg-white text-black font-medium hover:bg-gray-100 transition-colors duration-200"
        >
          Hablemos
          <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </div>
  </section>
</Layout>

<style>
  .prose {
    color: #374151;
    line-height: 1.75;
  }
  
  .prose h1 {
    font-size: 1.875rem;
    font-weight: 500;
    color: #000;
    margin-top: 3rem;
    margin-bottom: 2rem;
    line-height: 1.2;
  }
  
  .prose h2 {
    font-size: 1.5rem;
    font-weight: 500;
    color: #000;
    margin-top: 3rem;
    margin-bottom: 1.5rem;
    line-height: 1.3;
  }
  
  .prose h3 {
    font-size: 1.25rem;
    font-weight: 500;
    color: #000;
    margin-top: 2rem;
    margin-bottom: 1rem;
    line-height: 1.4;
  }
  
  .prose p {
    margin-bottom: 1.5rem;
    line-height: 1.75;
  }
  
  .prose ul {
    margin-bottom: 1.5rem;
    padding-left: 1.5rem;
    list-style-type: disc;
  }
  
  .prose li {
    margin-bottom: 0.5rem;
    line-height: 1.75;
  }
  
  .prose strong {
    color: #000;
    font-weight: 500;
  }
  
  .prose a {
    color: #000;
    text-decoration: underline;
    transition: color 0.2s;
  }
  
  .prose a:hover {
    color: #4b5563;
  }
  
  .prose code {
    background-color: #f3f4f6;
    color: #1f2937;
    padding: 0.125rem 0.25rem;
    border-radius: 0.25rem;
    font-size: 0.875rem;
  }
  
  .prose blockquote {
    border-left: 4px solid #d1d5db;
    padding-left: 1.5rem;
    font-style: italic;
    color: #4b5563;
    margin: 1.5rem 0;
  }
  
  /* Asegurar que el primer h1 no tenga margen superior excesivo */
  .prose > *:first-child {
    margin-top: 0 !important;
  }
</style>